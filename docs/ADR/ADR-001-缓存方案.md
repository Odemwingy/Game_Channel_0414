# ADR-001：缓存方案选型

## 状态
已采纳

## 决策
采用进程内 `Map/LRU` 作为缓存层，不引入 Redis。

## 背景
- 机载离线环境并发有限。
- 系统目标是单容器、低依赖、易运维。

## 影响
- 优点：部署简单、资源占用低、减少组件故障面。
- 代价：跨进程共享能力弱（当前架构可接受）。

## 落地要求
- 缓存键命名规范化。
- 缓存失效策略统一。
- 关键状态仍以 SQLite 持久化为准。
