# ADR-002：进程编排方案

## 状态
已采纳

## 决策
采用 `PM2` 管理 Node.js 业务进程（API + WebSocket），Nginx 独立前置。

## 背景
- 需要统一重启、日志、健康状态管理。
- 文档存在 `supervisord` 与 `PM2` 双口径，需要收敛。

## 影响
- 优点：Node 进程治理能力更强，日志管理统一。
- 代价：镜像内保留 PM2 运行时依赖。

## 落地要求
- PM2 进程配置文件版本化管理。
- 日志轮转与最大保留策略明确。
- 异常退出自动拉起并记录告警事件。
